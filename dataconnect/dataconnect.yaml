specVersion: "v1beta"
serviceId: "LearnLingo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "LearnLingo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
